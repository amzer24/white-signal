# Fourth Dish revisit

The Upper Amplifier now displays a high route requiring permanent Air Jump. Its ledge is 82 logical pixels above the lower platform: beyond the base single jump, with margin for the second jump. An actual controller test verifies reaching it without Dash. Existing exploration saves retain their current location and can revisit after Drowned.

The Fourth Dish archive is optional and replayable. A memory shows an Operator isolating an intact branch while a workshop light remains stable. The visual replay does not alter collision, consume an item or increment a mandatory collection quota. `fourth_archive` persists separately from all feeder-completion flags. Death and reload preserve discovery; replay remains available.

The overview map includes the upper branch and distinguishes its unvisited state. Capture review covered the visible ledge, replay, isolated state and map. Tests cover ability gating, real Air Jump landing, optional reward isolation, replay and save persistence. Field, Drowned, Stand and exploration regressions pass.

This completes the first optional Field revisit, not the final campaign or art pass. Flats, Wire, Array, Approach, Gate and broad polish remain required.
