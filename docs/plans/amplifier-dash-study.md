# Does acquiring Dash teach its use?

Current evidence: the production Amplifier grants Dash at an elevated control and checks ownership at its floor-level exit. The exit does not require applying the new movement. The Field already has a lift/catch rerouting puzzle, reflector alignment, an intake strike and hub shortcuts; duplicating those actions would add length without teaching the new verb.

The isolated amplifier_dash_study.tscn puts the protocol on a launch shelf, a receiving ledge across a 110px gap, and a safe floor below. A control on the receiving ledge adds return steps, making the consequence visible and shortening later attempts. Existing controller, no air jump, no enemy, no damage, no timer, no campaign persistence. R resets position while retaining in-memory discoveries.

Run with Godot --path white-signal-production res://prototypes/amplifier_dash_study.tscn. Startup smoke passed. The gap is a hypothesis, not a validated Dash requirement: measure early/late dash windows, normal-jump bypasses, retry travel, reverse traversal and controller input before integrating. Human comprehension remains untested. No live change. The prototype is retained locally outside the campaign and excluded by the existing prototype export exclusion; there is no Git repository for a throwaway branch.


Measured crossing: the initial 130px gap accepted only dash frames 25 and 30 among samples 0,5,10,15,20,25,30. Shortening to 110px admitted frames 15,20,25,30 while the sampled normal jump still landed on the safe floor. These are discrete samples from launch x229, jump release frame15, at 60Hz, not proof of every possible bypass or a continuous success window. Logs: test-user/amplifier-crossing.log and amplifier-crossing-tuned.log. Actual protocol acquisition, return route and human comprehension remain to be observed before integration. Prototype shutdown reports resource leaks; this is not release evidence.

Full route observation now passes with 0 missed observations: climb from the entry, collect the actual Dash control, walk to the launch edge, jump/dash to the receiving shelf, activate release, reset to the safe floor, ascend the new return steps, and traverse back to the entry. Dash and the release remain available after the prototype reset. Before/after blockouts rendered; the route is mechanically viable, but visual instruction and landmark art remain unfinished. Human comprehension is not established by scripted input.

Integration must retain amplifier as the saved room ID and existing gallery/return connections. Move the return exit onto the receiving shelf; release should persist and reconstruct steps without granting a new ability. Preserve a floor-level route for profiles that already had Dash before this teaching sequence existed. Distinguishing these from fresh acquisition needs an explicit persistent training-start marker written before acquisition, with failed-write and old-profile cases verified. Do not infer old completion merely from Dash after every pickup, which would deploy the bypass immediately and defeat the teaching goal. Both full campaign journeys will need the real crossing, not teleported fixtures.

Staging integration uses a simpler atomic pickup: dash and amplifier_training commit together, so no separate entry write is needed. Existing Dash profiles without that marker retain a floor exit and deployed steps. Fresh profiles use the upper exit; amplifier_released reconstructs the shortcut. Failed pickup, saved return steps and repeated interaction with legacy Dash passed amplifier_save_test.gd.

Both full campaign journeys now perform the actual first Dash crossing and release via input, then continue through all four feeder projects, Source and ending reconstruction: 0 failures for Drowned-first and Wire-first. Shutdown still reports resource leaks, which remain a separate release issue. Added etched launch arrows, a receiving marker and a steady light with a visible connection to the release. Render inspection and live installation remain pending.
